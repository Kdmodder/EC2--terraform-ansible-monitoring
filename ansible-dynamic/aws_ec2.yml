plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  instance-state-name: running
  tag:Environment: dev

hostnames:
  - private-ip-address

compose:
  ansible_host: public_ip_address

groups:
  workers: "'worker' in tags.Name"
  control: "'control' in tags.Name"

